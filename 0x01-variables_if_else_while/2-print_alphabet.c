#include <stdio.h>
/**
* main - Prints all alphabet in small letters
* prints all alphabet in small letters.
(*
* Return: 0 on success.
*/
int main(void)
{
char ch;
char cf = a;
char cb = z;
for (ch = cf ; ch <= cb ; ch++)
{
putchar (ch);
}
return (0);
}
